import { CodeBlock, ConfigBlock } from "@src/helpers/DocBlocks";
import Radio from "./Radio";

# Radio.Item

> A clickable UI element with active and hover states. Indicates to the user that it's clickable.

<CodeBlock defaultValue={true}>
  {(value, onChange) => (
    <Radio.Item label="Node Type" value={value} onChange={onChange} />
  )}
</CodeBlock>

## Configuration

<ConfigBlock of={Radio.Item} />

## Examples

<CodeBlock title="Default" defaultValue={false}>
  {(value, onChange) => (
    <Radio.Item label="Node Type" value={value} onChange={onChange} />
  )}
</CodeBlock>

<CodeBlock title="Disabled" defaultValue={false}>
  {(value, onChange) => (
    <Radio.Item label="Node Type" value={value} onChange={onChange} disabled />
  )}
</CodeBlock>
